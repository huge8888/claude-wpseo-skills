# ===========================================
# SEO WordPress Manager
# ===========================================
WP_GRAPHQL_URL=https://your-site.com/graphql
WP_USERNAME=your-username
WP_APP_PASSWORD=your-application-password

# ===========================================
# CTA Injector (Astro)
# ===========================================
# Path to your Astro content directory
CONTENT_PATH=./dist

# ===========================================
# Link Analyzer
# ===========================================
# Path to your built static site (works on local files, no remote fetching)
DIST_PATH=./dist

# Optional: your domain, used to distinguish internal vs external links
# (e.g., links to your-site.com are internal, others are external)
SITE_DOMAIN=your-site.com

# HTTP validation settings (for checking external links)
HTTP_TIMEOUT=5
HTTP_MAX_WORKERS=20
RETRY_COUNT=1

# ===========================================
# General Settings
# ===========================================
# Safety first: preview changes before applying
DRY_RUN=true
